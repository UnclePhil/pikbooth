<header>
  <div class="navbar navbar-dark bg-dark shadow-sm fixed-top">
    <div class="container-fluid d-flex justify-content-between">
      <a href="/booth" class="navbar-brand d-flex align-items-center">
        <img src="/img/pikbooth.png" height=40px > {{#if cfg.mode}} &nbsp;({{cfg.mode}}) {{/if}} 
      </a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarHeader" aria-controls="navbarHeader" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
    </div>
  </div>
  <div class="collapse navbar-collapse bg-dark" id="navbarHeader">
    <div class="container">
      <div class="row">
        <div class="col-sm-8 col-md-7 py-4">
          <p ><img src="/img/pikbooth.png"> is a photobooth application running on a fleet of RaspberryPi and managed through Balena.io interface</p>
          <p >Pictures can be take through DSLR, piCam and usb webCam</p>
          <p >Made with <3 by Unclephil </p>
          <p > Configuration:
            <ul>
              <li>Actual MODE is: {{#if cfg.mode}} ({{cfg.mode}}) {{/if}} </li>
              <li>Actual SSID is: {{#if ssid}} ({{ssid}}) {{/if}} </li>
              <li>Actual   IP is: {{#if ipaddr}} ({{ipaddr}}) {{/if}} </li>   
          </ul>
          </p>
       </div>
      </div>
    </div>
  </div>
</header>
